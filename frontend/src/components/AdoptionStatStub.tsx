"use client";

/**
 * Placeholder for a real adoption metric (D3.1).
 * Replace the copy with a live counter once traffic supports an honest number
 * distinct from the Plaid 12,000+ institutions network stat.
 */
export default function AdoptionStatStub() {
  return (
    <div className="mt-10 text-center">
      <p className="text-sm text-muted-foreground">
        {/* FLAG: replace with a real usage number when available */}
        A live &ldquo;financial questions answered this month&rdquo; count will
        appear here once we can publish an honest figure — separate from the
        institution network above.
      </p>
    </div>
  );
}
