// TODO: Stripe service is temporarily disabled due to missing dependencies
// This file requires:
// - ../config/stripe (missing)
// - ../types/stripe (missing)
// - Proper Prisma client export

// Placeholder export to prevent import errors
export const stripeService = {} as any;
