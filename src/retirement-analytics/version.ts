/**
 * Increment whenever classification, simulation, or published metric semantics
 * change without changing the checked-in historical return dataset.
 */
export const RETIREMENT_ANALYSIS_VERSION = 4 as const;
